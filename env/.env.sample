# ============================================
# WordPress 開発環境設定ファイル
# ============================================
# このファイルを .env にコピーして使用してください
# cp env/.env.sample env/.env

# ============================================
# MySQL データベース設定
# ============================================
# データベース名
MYSQL_DATABASE=wordpress

# データベースユーザー名
MYSQL_USER=wordpress

# データベースパスワード（本番環境では強力なパスワードに変更してください）
MYSQL_PASSWORD=wordpress

# MySQL rootパスワード（本番環境では強力なパスワードに変更してください）
MYSQL_ROOT_PASSWORD=rootpassword

# ============================================
# WordPress データベース接続設定
# ============================================
# データベースホスト（通常は 'db' コンテナ名）
WORDPRESS_DB_HOST=db

# データベースユーザー名（MYSQL_USERと同じ値に設定）
WORDPRESS_DB_USER=wordpress

# データベースパスワード（MYSQL_PASSWORDと同じ値に設定）
WORDPRESS_DB_PASSWORD=wordpress

# データベース名（MYSQL_DATABASEと同じ値に設定）
WORDPRESS_DB_NAME=wordpress

# ============================================
# WordPress インストール設定
# ============================================
# WordPressのURL（ローカル開発環境の例）
# 例: http://localhost:8080
# 例: http://nyardpress.local
WORDPRESS_URL=http://localhost:8080

# WordPressサイトのタイトル
WORDPRESS_TITLE=Nyardpress

# WordPress管理画面のユーザー名（本番環境では変更してください）
WORDPRESS_ADMIN_USER=admin

# WordPress管理画面のパスワード（本番環境では強力なパスワードに変更してください）
WORDPRESS_ADMIN_PASSWORD=admin

# WordPress管理画面のメールアドレス
WORDPRESS_ADMIN_EMAIL=admin@example.com

# ============================================
# WordPress ポート設定
# ============================================
# WordPressコンテナのポート（ホスト側のポート）
# 例: 8080 → http://localhost:8080 でアクセス
# 例: 3000 → http://localhost:3000 でアクセス
WORDPRESS_PORT=8080

# ============================================
# プロジェクト設定
# ============================================
# プロジェクト名（コンテナ名、ボリューム名、ネットワーク名に使用）
PROJECT_NAME=nyardpress

# テーマ名（入力なしでPROJECT_NAMEと同じ値が使用されます）
THEME_NAME=nyard_theme
